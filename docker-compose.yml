version: "3.9"
services:
  hexo-blog:
    build:
      context: .
    container_name: hexo-blog
    image: hexo-blog:latest
    restart: always
    environment:
      HEXO_SERVER_PORT: 4080
    ports:
      - '8080:4080'
    volumes:
      - ./data/blog:/root/blog
